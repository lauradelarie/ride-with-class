<%= form_for @passengers do |f| %>

   <%= f.label :name %>
   <%= f.text_field :name %>

   <%= f.label :age %>
   <%= f.number_field :age %>

   <%= f.label :gender %>
   <%= f.text_field :gender %>

   <%= f.submit (@passengers.new_record? ? "Create" : "Update") %>
<% end %>
