<h1>hello passengers</h1>
<% @passengers.each do |passenger| %>
  <p><%= link_to passenger.name, passenger_path(passenger.id) %></p>
<% end %>

<%= link_to "[New Passenger]", new_passenger_path %>
<%= link_to "[All Passenger]", passengers_path %>
